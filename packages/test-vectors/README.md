# `@coduckai/flags-test-vectors`

Language-neutral golden values for the CoDuck Flags UTF-8 FNV-1a 32-bit hash and 10,000-bucket
assignment contract. The vectors cover Unicode strings and canonical numeric representations.
Every future language SDK must reproduce these values exactly.
